// Package api exposes authenticated Cloud Tracking authority, client-state,
// and current-observation endpoints. It validates transport contracts and
// delegates business decisions to tracking packages; it never executes a
// scanner or accepts an arbitrary executable URL.
package api

// Package worker owns the bounded QuickJS and network/session execution
// boundary used by tracking scanners. Source scripts receive only the
// capabilities explicitly granted by the scanner contract.
package worker
